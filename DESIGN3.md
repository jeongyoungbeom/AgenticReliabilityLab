# DESIGN3 — 선언형 테스트 명세와 외부 실행

`DESIGN.md`(Phase 0–10.7)와 `DESIGN2.md`(Phase 11–16)에 이어지는 설계다.
형식 상세는 같은 저장소의 `TEST_SPEC.md`에 있고, 이 문서는 **왜 이렇게 바꾸는지와 무엇을 만드는지**를 다룬다.

---

## 1. 왜 이 문서가 필요한가

DESIGN2까지의 구조에는 두 가지 벽이 있다.

**① Target이 우리 계약대로 코드를 짜야 실행된다.**
`TEST_HARNESS_V1`은 테스트 종류마다 엔드포인트가 필요하다. 도입 장벽이 높고,
AI가 새 테스트를 제안할 때마다 Target 팀이 코드를 짜야 한다.

**② 판정기가 코드에 박혀 있다.**
`StockConcurrencyResultCollector`에 불변식 6개가 Kotlin으로 하드코딩돼 있고
`ExperimentType`은 닫힌 목록이다. **AI가 찾아낼 수 있는 테스트가 "우리가 이미 판정기를 짜둔 것"으로 제한된다.**

②가 특히 문제다. 이 제품의 이유는 **"내가 생각 못 한 테스트를 찾아주고, 결과를 보고 어떻게 고칠지 알려주는 것"**인데,
찾아낸 테스트를 실행할 수 없으면 AI의 제안은 실행되는 테스트가 아니라 **할 일 목록**이 된다.

---

## 2. 결정 사항

| 항목 | 결정 |
|---|---|
| 실행 방식 | **외부 실행을 기본**으로 추가. `TEST_HARNESS_V1`은 정밀 옵션으로 유지 |
| Target에 요구하는 것 | **읽기 전용 상태 조회 API 하나**(`/harness/state`)가 기본. 나머지는 선택 |
| 정리 | **환경 리셋 + 리셋 검증.** 레코드 단위 삭제를 Target에 요구하지 않는다 |
| 관측 | Target의 기존 텔레메트리 (Prometheus / OTel) + `/state` |
| 불변식 | **코드 → 데이터.** LLM이 제안, 사람이 승인, 결정적 엔진이 평가 |
| 판정 | 결정적. **LLM 금지** |
| 명세 형식 | **JSON + JSON Schema.** LLM 구조화 출력 강제, 스키마로 1차 검증 |
| 표현식 평가 | **CEL.** 직접 파서를 만들지 않는다. 등록한 함수만 존재하는 샌드박스 |
| LLM 역할 | 테스트 발굴 + 판정 기준 제안 + 결과 해석·개선 제안 |
| 배포 형태 | 온프레미스 지향. 로컬 모델(Ollama) 구조를 그대로 활용 |
| 결정적 인터리빙 재현 | **범위 제외** |

### 왜 정리를 Target에 안 맡기는가

주문 하나를 만들면 흔적이 8개 서비스에 남는다(결제·정산·배송·알림). ARL은 자기가 직접 만든 것만 알고
하위 흔적은 알지 못한다. 취소는 보상 이벤트를 또 퍼뜨려 흔적을 늘리고, 비동기라 삭제 타이밍도 맞지 않는다.

**정리는 애플리케이션이 아니라 환경 수준에서 푼다.** 고객에게 요구하는 것이
"정리 코드를 짜세요"가 아니라 **"리셋 방법을 알려주세요"**가 된다.

### 왜 `/state` 하나만 요구하는가

fixture는 진짜 API로 만들 수 있고(인증만 있으면), 정리는 환경 리셋으로 대체된다.
**밖에서 대체 불가능한 것은 내부 상태 관측 하나뿐이다.** Redis 예약 재고나 여러 서비스 DB를
가로지르는 값은 API로 나오지 않는다.

그리고 `/state`는 만들기 쉽다. **읽기만 하므로 위험하지 않고**, 도메인 로직이 거의 없다.

---

## 3. 구조 — 선언형 명세가 축이다

```
            ┌─────────────────────────┐
LLM 제안 →  │   선언형 테스트 명세      │  ← 사람이 검토·승인
            └─────────────────────────┘
                      ↓
    ┌─────────────┬───────────┬──────────┐
    ↓             ↓           ↓          ↓
 실행 어댑터    관측 소스    판정 엔진    분석
 · 외부 API    · API 응답   (하나)      (LLM)
 · Harness     · /state
 · 장애 주입    · Prometheus
 · 인프라 제어  · OTel 트레이스
```

명세 형식만 제대로 잡으면

- 새 테스트 = 명세 추가 (**코드 0**)
- 새 실행 방식 = 어댑터 추가
- 새 관측 소스 = 소스 추가
- LLM 제안 = **명세를 생성하는 일**

**형식을 잘못 잡으면 전부 다시 만든다. 이 프로젝트의 유일한 진짜 위험이다.**

---

## 4. 흐름

### 처음 한 번

1. 연결 정보 입력 — 주소, 테스트 계정, 리셋 명령, (선택) `/state` 주소, (선택) 관측 소스
2. OpenAPI 붙여넣기 → Profile 초안 자동 생성 → 검토 후 활성화

### 매번

| | 누가 |
|---|---|
| 문서에서 이해 모델 추출 | 자동 (규칙) |
| **테스트 명세 생성** | **자동 (LLM)** |
| 검증기가 거름 | 자동 |
| **승인** | **사람 — 유일한 개입** |
| 리셋 → 리셋 확인 → fixture → 워크로드 → 관측 | 자동 |
| 불변식 판정 | 자동, 결정적 |
| 원인 가설·개선 제안 | 자동 (LLM) |

### 그다음부터

승인된 명세는 저장된다. 다음 배포부터는 **사람 개입 0**으로 자동 실행되는 회귀 테스트 자산이 된다.

승인 단계는 **"이 테스트를 돌릴까요?"가 아니라 "이 기준으로 판정할까요?"**다.
불변식이 틀리면 판정도 틀리기 때문이다. 결정적이라는 건 "항상 옳다"가 아니라 "항상 같다"는 뜻이다.

---

## 5. 구현 Phase와 완료 조건

앞의 넷은 순서를 바꿀 수 없다. 관측 없이 판정을 일반화할 수 없고, 실행 없이 관측할 것이 없다.

### Phase 17 — 수직 슬라이스

**목표:** 명세 하나를 파싱부터 판정까지 끝까지 완주시킨다. 각 조각의 가장 초라한 버전으로.

- 명세 JSON Schema + CEL 평가 환경 구성 (등록 함수, 매크로 제외)
- 검증기 — 스키마가 못 잡는 의미 규칙 (경로·변수·상한)
- Target 인증 (secret reference, Runner에서만 해석)
- 외부 실행 어댑터 — fixture 생성 / 워크로드 / 응답 수집
- 관측 소스: API 응답만
- 불변식 평가 엔진
- 환경 리셋 + **리셋 검증**

**완료 기준:** 동시성·멱등성·정합성 세 명세가 코드 수정 없이 실행되고 판정된다.
세 개를 동시에 통과시켜야 형식이 검증된다. 하나로만 맞추면 두 번째에서 무너진다.

### Phase 18 — 관측 확장

**목표:** 내부 상태를 본다.

- `/harness/state` 클라이언트 + capability 협상
- Prometheus 쿼리 소스
- 관측 실패의 국소화 — 못 읽은 값에 딸린 불변식만 판정 불가로 내리고 나머지는 정상 판정

**완료 기준:** `/state` 없는 Target에서도 나머지 불변식이 정상 판정된다.

### Phase 19 — 트레이스

**목표:** "왜 그랬는지"를 본다.

- OTel/Tempo 트레이스 조회
- 시간축 관측 — 구간 목록, `겹침없음`, `순서보장`
- 분석 프롬프트에 트레이스 근거 연결

**완료 기준:** "재고가 -3"이 아니라 "3·7·9번이 같은 시각에 예약을 읽었고 반영이 340ms 늦었다"가 나온다.
**AI 개선 제안의 품질이 여기서 갈린다.**

### Phase 20 — LLM 제안

**목표:** 제품의 이유가 켜진다.

- `TestCandidateProposalModel` 구현 (기존 Ollama 연동 재사용)
- LLM이 명세를 생성. 검증기가 거름
- 규칙 기반 목록과 **나란히 놓고 품질 비교**
- `근거: 없음` 임계값 강조 표시

**완료 기준:** 규칙만 돌린 목록에 없는 유효한 테스트를 LLM이 하나 이상 찾아낸다.
**못 찾으면 붙이지 않는다.** 규칙으로 충분한데 LLM을 얹는 것은 실속 없는 장식이다.

### Phase 21 — 장애 주입

**목표:** 재시도·복구 계열을 연다.

- `/harness/fault` 계약 + 클라이언트
- 주입 조율 — TTL 필수, finally 해제, 다음 실행 전 활성 장애 확인 후 **거부**
- 인프라 제어 어댑터 (Docker/K8s) — 별도 어댑터로 분리
- `LOCAL`/`TEST` 외 환경에서는 기능 자체 차단

**완료 기준:** 실행이 중간에 죽어도 장애가 남지 않는다. 남아 있으면 다음 실행이 거부된다.

### Phase 22 — 되먹임과 자산화

**목표:** 오판이 반복되지 않고, 승인된 명세가 회귀 자산이 된다.

- 오판 신고 → 예외 초안 → 검증 → 승인 → 명세 버전 상승
- 명세 버전 관리 — Profile 변경 시 참조 재검증
- 저장된 명세의 자동 재실행 (CI 연결)

**완료 기준:** 같은 오판이 두 번 보고되지 않는다. 배포 후 승인 없이 회귀 테스트가 돈다.

---

## 6. sideProject(파일럿)에 필요한 것

| | 필수 | 작업량 |
|---|---|---|
| 테스트 계정 (판매자·구매자) | ✅ | 계정 생성 |
| `GET /harness/state` | 권장 | 읽기 전용. 여러 저장소 값을 JSON으로 |
| 환경 리셋 | ✅ | 코드 아님. `docker compose down -v && up` |
| OpenTelemetry | 선택 | Phase 19에서 필요. 없으면 Prometheus 15초 해상도 한계 |
| `POST /harness/fault` | 선택 | Phase 21에서 필요. 주입 지점마다 훅 |

**아무것도 안 만들어도 도입은 된다.** 가용성·계약 검증은 지금도 되고,
동시성도 최종 DB 재고로 부분 확인이 된다. `/state`는 "더 보고 싶으면" 자연스러운 다음 걸음이다.

---

## 7. 안전 경계 변경

ARL이 직접 쓰기 요청을 보내게 되므로 기존 문구가 바뀐다.

**이전:** ARL은 Target을 변경하지 않는다.
**이후:** ARL은 **승인된 명세의 범위 안에서, `LOCAL`/`TEST` 환경에서만** Target 상태를 변경한다.

유지되는 통제

- `LOCAL`/`TEST` 외 실행 차단. 장애 주입·인프라 제어는 더 강하게 차단
- Profile이 선언한 경로·인증프로필·관측소스·장애 종류만 사용
- 실효 한계는 **Profile과 capability 중 작은 쪽**
- 확인 문구 승인, 위험도별 승인 수준, 감사 기록
- 정리 미확인 시 다음 실행 차단

새로 추가되는 통제

- **되돌릴 수 없는 상태를 만들 수 없다** — TTL·최대유지 없는 장애·인프라 사용 거부
- **엔진이 finally로 전부 복구** — 명세가 빠뜨려도
- **다음 실행 전 잔여 확인** — 활성 장애나 미복구 대상이 있으면 거부
- **임의 코드 실행 금지** — 허용 함수 목록 밖은 거부
- **예외가 불변식을 무력화할 수 없다** — 되먹임이 판정을 지우는 데 쓰이면 안 된다

---

## 8. 무엇을 테스트할 수 있고 없는가

| | 외부 실행 | +`/state` | +트레이스 | +장애 주입 | +인프라 제어 |
|---|---|---|---|---|---|
| 가용성·계약·경계값 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 멱등성 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 동시성 (결과) | 부분 | ✅ | ✅ | ✅ | ✅ |
| 동시성 (내부 불일치) | ❌ | ✅ | ✅ | ✅ | ✅ |
| 정합성·수렴 | 부분 | ✅ | ✅ | ✅ | ✅ |
| **원인 정밀 규명** | ❌ | ❌ | ✅ | ✅ | ✅ |
| 재시도·복구 | ❌ | ❌ | ❌ | ✅ | ✅ |
| 인프라 장애 | ❌ | ❌ | ❌ | ❌ | ✅ |

### 끝까지 안 되는 것

- **결정적 인터리빙 재현** — 관측이 아니라 실행 흐름 제어가 필요하다.
  Target 아키텍처를 바꾸거나 프로덕션 코드에 테스트 훅을 심어야 하므로 범위 밖으로 둔다.
  대신 **반복 시행 + 지연 흔들기**로 확률을 올린다.
- **어디에도 드러나지 않는 내부 상태** — 계측을 추가하면 열리므로 고정된 한계는 아니다.
- **기준값을 사람이 정해야 하는 것** — "응답 200ms 이내"의 200이 어디서 오는가.
- **문서에 없는 도메인 규칙** — 이것이 진짜 천장이다.
  "VIP는 재고가 없어도 예약 주문이 된다"가 문서에 없으면 LLM은 모르고,
  **정상 동작을 버그로 판정한다.** 그래서 오판 되먹임(Phase 22)이 선택이 아니라 필수다.

### 정직하게 말해야 할 것

**확률적으로 찾는다. 못 찾았다고 없는 것이 아니다.**
관측이 안 되면 통과가 아니라 **판정 불가**로 남긴다. 이 구분을 흐리면 신뢰성 도구가 신뢰를 잃는다.

---

## 9. 형식과 평가기 — 확정

### 명세는 JSON, 스키마로 강제한다

`TEST_SPEC.md`의 예시는 읽기 쉬우라고 YAML로 적었을 뿐이고 **정식 형식은 JSON**이다.

- LLM에게 **구조화 출력**을 강제한다. 스키마를 어기면 모델이 다시 시도한다
- 구조·타입·필수값 검사가 스키마로 자동화된다. 검증기는 의미 규칙만 맡는다
- 사람은 명세 원문을 읽지 않는다(화면에서 풀어 보여준다). **가독성은 선택 기준이 아니다**
- Phase 5 분석 경로가 이미 JSON 강제 + 엄격 검증기 패턴이라 일관성도 맞는다

### 표현식은 CEL을 쓴다

조건과 식을 위해 파서·평가기·샌드박스를 직접 만들지 않는다.
**CEL(Common Expression Language)** 은 튜링 완전하지 않아 무한 루프가 불가능하고,
평가 환경에 **등록한 함수만 존재**한다. 쿠버네티스가 admission policy에 쓰는 것과 같다.

따라오는 제약이 하나 있다. **CEL 식별자는 ASCII만 허용**하므로
(`[_a-zA-Z][_a-zA-Z0-9]*`) 관측 이름을 영문으로 쓰고 표시용 한글은 `설명`에 둔다.
식별자와 표시 이름을 가르는 것은 CEL과 무관하게도 맞는 설계다.

CEL 매크로(`all`, `exists`, `map`, `filter`)는 평가 환경에서 뺀다.
표현력이 늘면 LLM이 검증하기 어려운 식을 만들고, 승인 화면에서 사람이 읽기도 어려워진다.

### 아직 열려 있는 것

- **개발용 최소 Target** — sideProject는 리셋에만 약 2분이 걸린다.
  명세 하나 고치고 2분 기다리는 것을 하루에 수십 번 하게 된다.
  재고 하나짜리 작은 앱을 개발용으로 두고 sideProject는 검증용으로 쓰는 편이 빠를 수 있다.
  부수 효과로 **형식 이식성 검증(10절)도 절반은 해결된다.**
- **LLM 생성 방식** — 명세 전체를 생성할 것인가, 카테고리별 템플릿의 빈칸만 채우게 할 것인가.
  후자가 생성 정확도는 높지만 발견할 수 있는 테스트의 폭이 줄어든다.

---

## 10. 남은 검증

**형식을 다른 프로젝트로 검증하지 않았다.** sideProject 하나에만 맞춰봤다.
도메인·인증 방식·아키텍처가 다른 프로젝트로 명세를 두어 개 써보기 전까지 형식은 잠정이다.

확인이 필요한 것

- 역할 기반이 아닌 인증(API 키, OAuth 스코프)이 `인증프로필`에 들어가는가
- 단일 서비스 프로젝트에서 `관측소스` 구조가 과하지 않은가
- REST가 아닌 인터페이스에 `프로토콜` 확장 자리가 충분한가

명세 형식은 코드보다 먼저 확정되어야 한다. 여기서 몇 시간 쓰는 것이 나중에 몇 주를 아낀다.
