# 작업 지침

## 기본 원칙

- 사용자의 언어를 따르되 별도 요청이 없으면 한국어로 소통하고 결과를 먼저 설명한다.
- 확인된 사실, 가정, 검증하지 못한 내용을 구분한다.
- 중요한 설계 판단과 가정은 이유와 함께 간결하게 공유한다.
- 안전 규칙 아래 현재 활성 작업에 적용되는 제약을 따른다. 후속 요청이 이전 요청을 대체하거나 구체화하면 최신 요청을 반영하고, 실제 충돌이 결과를 크게 바꿀 때만 질문한다.
- 도구의 내부 동작을 불필요하게 나열하거나 중간 결과를 최종 완료처럼 보고하지 않는다.

## 정보별 Source of Truth

하나의 문서를 모든 정보의 기준으로 쓰지 않는다. 충돌하면 아래 역할대로 판단한다.

- `TASK.md`와 명시적 요구사항 문서 = **무엇을 만들어야 하는가.** 코드가 이와 다르면 코드가 정답이 아니라
  요구사항 불일치를 의심한다.
- 실제 repository와 Git 상태 = **현재 무엇이 구현되어 있는가.** 문서가 완료라고 주장해도 코드를 직접 확인한다.
- 테스트 결과 = **특정 revision에서 무엇이 검증되었는가.** 그 실행 이후 관련 코드가 바뀌었으면
  이전 결과를 현재의 검증 완료로 취급하지 않는다.
- `HANDOFF.md` = **지금 어디까지 진행됐는가.** 참고 정보이며 코드나 요구사항보다 우선하지 않는다.
- `DECISIONS.md` = **유지해야 하는 확정 설계 판단.** 명시적 요구사항과 충돌할 수 없다.
- `reviews/<task>/REVIEW.md` = 독립 리뷰가 발견한 문제. `RESOLUTION.md` = 그 finding을 현재 코드 기준으로
  어떻게 판단하고 처리했는가.
- `DESIGN.md`~`DESIGN4.md`, `TEST_SPEC.md`, `TARGET_REQUIREMENTS.md` = 설계 과정과 상세 참고 자료.
  별도로 명시되지 않는 한 현재 제품 계약의 최종 기준으로 취급하지 않는다.

같은 정보를 여러 문서에 복제하지 않는다.

## 작업 유형별 Workflow

현재 역할에 필요한 workflow만 읽고 적용한다. 세 skill을 한 세션에서 동시에 적용하지 않는다.

- 구현 / 수정 / 리팩터링 / 버그 수정
  - `.agents/skills/develop-with-user/SKILL.md`
- 구현 완료 후 독립 코드 리뷰
  - `.agents/skills/independent-review/SKILL.md`
- 독립 리뷰 결과 검증 및 반영
  - `.agents/skills/apply-review/SKILL.md`

canonical workflow는 `.agents/skills/`에 있다. `.claude/skills/`는 같은 파일을 가리키는 얇은 adapter이며
내용을 복제하지 않는다.

## 작업 범위

- 분석·설명·리뷰·진단만 요청받았다면 코드를 수정하지 않는다.
- 구현·수정을 요청받았다면 요청되거나 확인된 범위에서 구현과 검증까지 완료한다.
- 요청과 직접 관련된 파일만 수정하고 기존 사용자 변경을 보존하며, 요청 없이 되돌리거나 덮어쓰지 않는다.
- 요청하지 않은 기능, 리팩터링, 의존성 또는 목적을 추가하지 않는다.
- `AGENTS.md`와 `SKILL.md`는 명시적인 요청 없이 수정하지 않는다.
- 요구사항 문서와 기존 테스트는 승인된 동작 계약 변경 또는 요구사항의 의도된 동작 반영에 필요할 때만 수정하고 이유를 알린다.
- 테스트를 임의로 삭제하거나 통과만을 위해 기대값을 약화하지 않는다.

## 조사와 진행

- 수정 전에 적용 지침, 요구사항, 저장소 구조, 관련 코드와 테스트를 확인한다.
- 빌드·실행·테스트 명령은 README, 빌드 파일과 래퍼에서 확인하고 추측하지 않는다.
- 기존 구조, 명명법, 오류 처리와 코딩 스타일을 우선한다.
- 결과를 크게 바꾸는 불확실성, 데이터 손실, 외부 변경 위험이 있을 때만 질문한다.
- 공개 API나 중요 설계 변경이 요청에 없거나 의미 있는 선택지가 남아 있으면 영향과 대안을 설명하고 확인한다. 이미 명시적으로 요청됐고 안전하게 진행할 수 있으면 알리고 계속한다.
- 안전한 가정으로 진행할 수 있으면 이를 알리고 불필요하게 멈추지 않는다.
- 단계별 승인을 요청받지 않았다면 진행 상황을 짧게 공유하며 완료까지 진행한다.
- 필요한 정보·권한·사용자 선택이 없어 안전하게 진행할 수 없을 때만 멈추고, 확인한 내용과 필요한 결정을 알린다.

## 구현 원칙

- 완료 조건을 만족하는 가장 단순한 설계를 선택한다.
- 불필요한 추상화, 계층, 범용화와 새로운 의존성을 피한다.
- 입력값과 외부 데이터를 검증하고 오류 원인을 확인 가능한 형태로 처리한다.
- 인증정보와 비밀값을 코드, 로그, 명령 출력, 스크린샷과 완료 보고에 넣거나 노출하지 않는다.
- 테스트 통과나 구현 편의를 위해 인증, 권한, 입력 검증과 보안 통제를 우회하거나 약화하지 않는다.
- 새 의존성이 필요하면 기존 도구로 대체 가능한지, 프로젝트와 호환되는 버전인지 확인하고 관련 잠금 파일을 함께 갱신한다.
- 공개 API, 설정 또는 실행·운영 방법이 바뀌면 관련 문서를 함께 갱신한다.
- 임시 우회, 디버그 출력, 자리표시자와 사용하지 않는 코드를 남기지 않는다.

## 검증

- 변경 후 diff를 읽어 범위 이탈, 누락과 회귀 가능성을 확인한다.
- 변경에 비례해 관련 테스트, 빌드, 정적 검사 또는 직접 실행을 수행한다.
- API·데이터 로직은 정상값, 경계값과 실패 상황을 확인하고 UI는 가능한 경우 핵심 흐름을 직접 확인한다.
- 변경으로 발생했고 요청 범위 안에서 수정 가능한 검증 실패는 원인을 수정한 뒤 다시 실행한다. 기존 실패와 환경 문제는 구분해 보고한다.
- 수행하지 못한 검증은 성공으로 표현하지 않고 이유와 남은 위험을 알린다.

## 안전

- 요청 범위의 일반적인 파일 편집은 재승인 없이 진행한다.
- 삭제, 대량·비가역적 덮어쓰기나 이동, 실제 데이터·스키마 변경, 배포, 외부 메시지, Git commit·merge·push와 복구하기 어려운 작업은 요청에 명시됐다면 정확한 대상을 내부 확인한 뒤 진행하고, 명시되지 않았다면 사용자에게 확인한다.
- 사용자가 요청하지 않은 외부 시스템 변경을 수행하지 않는다.

## 완료 보고

- 완성된 결과와 주요 판단
- 변경한 핵심 파일
- 실행한 검증과 결과
- 실행 또는 확인 방법
- 남은 한계와 미검증 사항

간단한 작업은 위 항목을 억지로 나누지 말고 핵심만 보고한다.

## 작업 효율과 컨텍스트 관리

- 기본적으로 주 에이전트가 직접 작업하며, 단순 조사·구현·리뷰·테스트를 위해 서브 에이전트를 사용하지 않는다.
- 서브 에이전트는 서로 독립적인 작업을 병렬화했을 때 명확한 이점이 있고, 주 에이전트가 직접 수행하는 것보다 컨텍스트와 작업량을 실질적으로 줄일 수 있을 때만 사용한다.
- 하나의 문제를 여러 서브 에이전트에게 중복 조사·검토시키지 않는다. 독립적인 교차 검증이 완료 조건에 명시되었거나 중요한 위험을 확인해야 하는 경우는 예외로 한다.
- 서브 에이전트 사용 시 필요한 파일, 질문과 작업 범위를 최소화하고 저장소 전체 또는 불필요한 대화 맥락을 전달하지 않는다.
- 동시에 사용하는 서브 에이전트는 원칙적으로 2개 이하로 제한한다. 더 많은 병렬 작업이 필요한 경우 명확한 이점이 있을 때만 추가한다.
- 이미 확인한 파일과 정보를 이유 없이 다시 읽거나 검색하지 않는다. 파일이 변경됐거나 현재 판단에 필요한 정보가 부족한 경우에만 다시 확인한다.
- 저장소 전체를 무조건 탐색하지 않는다. 먼저 관련 파일과 심볼을 검색하고 필요한 범위만 읽은 뒤, 의존 관계를 따라 점진적으로 조사한다.
- 긴 파일은 전체 내용이 필요한 경우가 아니면 관련 구간만 읽는다.
- 동일한 검색, 빌드, 테스트 또는 검증을 결과 변화 없이 반복하지 않는다.
- 전체 테스트나 전체 빌드는 변경 영향이 넓거나 완료 검증에 필요한 경우 수행하고, 개발 중에는 가능한 한 관련 범위의 검증을 우선한다.
- 진행 보고와 완료 보고는 판단, 변경, 검증 결과에 필요한 내용만 포함하고 이미 공유한 내용을 반복하지 않는다.
- 단순한 작업에 장문의 계획, 과도한 분석 문서 또는 별도의 요약 산출물을 만들지 않는다.
- 추가 조사나 추상화가 결과의 정확성, 안전성 또는 완료 조건에 실질적으로 기여하지 않으면 현재 확인된 정보로 가장 단순하게 진행한다.
